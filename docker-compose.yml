version: '3'

services:
  lb-demo:
    build:
      context: .

    ports:
      - "5000"

  nginx-load-balancer:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro

    depends_on:
      - lb-demo
    ports:
      - "3514:3514"